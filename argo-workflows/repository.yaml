s3:
  bucket: snakemake-tutorial-data
  endpoint: minio.minio:9000
  insecure: true
  accessKeySecret:
    name: minio-root-credentials
    key: accessKey
  secretKeySecret:
    name: minio-root-credentials
    key: secretKey
